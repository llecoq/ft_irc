#include "Server.hpp"

// void Server::execution(Client& client) {

// 	std::string buffer = client.get_str_buf();
// 	// (void)buffer;

// 	// printf("buf :%s\n", client.get_buf());

// 	std::string index[9];

// 	for (int i = -1; i < 9 && index[i] != buffer;++i);
// 	void (Commands::*cmd_exec[9])(void);


// }